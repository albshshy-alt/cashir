# إصدار 1.0.7 — الحساب المحلي والتحديث داخل التطبيق

المالك: **Yusuf elbeses** — جميع الحقوق محفوظة.

## أول تشغيل

عند تشغيل التطبيق لأول مرة على أي كمبيوتر تظهر شاشة إنشاء الحساب. يختار المستخدم اسم المستخدم وكلمة المرور، ثم تُحفظ كلمة المرور محليًا بعد تحويلها إلى بصمة SHA-256. لا توجد كلمة مرور ثابتة مشتركة داخل النسخة الجديدة.

## التشغيل اللاحق

تظهر شاشة الدخول في كل تشغيل. الحساب محلي لهذا الكمبيوتر، ويستمر التطبيق في العمل دون إنترنت بعد إنشاء الحساب.

## التحديث داخل التطبيق

عند توفر إصدار جديد من GitHub Releases يظهر شريط داخل واجهة التطبيق يوضح رقم الإصدار ويحتوي زر «تحديث الآن». يبدأ التنزيل عند الضغط، يظهر تقدم التنزيل، ثم يعيد التطبيق تشغيل نفسه ويثبت التحديث تلقائيًا.

## مخزون المنتجات

من لوحة الإدارة يمكن إدخال كمية كل منتج. تُخصم الكمية عند إتمام وطباعة الفاتورة، وعند وصولها إلى صفر يتعطل المنتج وتظهر رسالة «انتهت الكمية».
