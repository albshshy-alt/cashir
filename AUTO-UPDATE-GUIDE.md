# تحديث نظام كاشير تلقائيًا

المالك: **Yusuf elbeses** — جميع الحقوق محفوظة.

تم إعداد التطبيق لاستخدام GitHub Releases عبر المستودع:

`https://github.com/albshshy-alt/cashir`

## آلية العمل

نسخة Windows المثبتة عبر NSIS تفحص وجود إصدار أحدث بعد تشغيل التطبيق. عند توفر إصدار جديد تظهر رسالة للمستخدم، ويمكنه الضغط على «تحديث الآن». يُنزّل التطبيق التحديث، يتحقق منه Electron، يعيد التشغيل، ويثبت الإصدار الجديد تلقائيًا.

## نشر تحديث جديد

1. عدّل ملفات التطبيق.
2. ارفع رقم `version` في `package.json`، مثل `1.0.4`.
3. أنشئ commit ثم tag بصيغة `v1.0.4`.
4. ادفع الـ tag إلى GitHub.
5. يبدأ GitHub Actions تلقائيًا على Windows، ويبني مثبت NSIS وينشره في GitHub Release.

مثال:

```bash
git add .
git commit -m "إضافة تحديث جديد"
git tag v1.0.4
git push origin main --tags
```

لا تستخدم النسخة المحمولة القديمة للتحديث التلقائي؛ يجب تثبيت نسخة NSIS الأولى حتى يعمل `electron-updater` بصورة صحيحة.
